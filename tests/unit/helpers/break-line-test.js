import { breakLine } from 'mobecallcentre/helpers/break-line';
import { module, test } from 'qunit';

module('Unit | Helper | break line');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = breakLine([42]);
  assert.ok(result);
});
