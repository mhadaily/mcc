import {momentCustomfmt} from 'mobecallcentre/helpers/moment-customfmt';
import {module, test} from 'qunit';

module('Unit | Helper | moment customfmt');

// Replace this with your real tests.
test('it works', function (assert) {
  let result = momentCustomfmt([42]);
  assert.ok(result);
});
