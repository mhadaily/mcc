import { trimValue } from 'mobecallcentre/helpers/trim-value';
import { module, test } from 'qunit';

module('Unit | Helper | trim value');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = trimValue([42]);
  assert.ok(result);
});
