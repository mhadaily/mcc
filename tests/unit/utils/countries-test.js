import countries from 'mobecallcentre/utils/countries';
import { module, test } from 'qunit';

module('Unit | Utility | countries');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = countries();
  assert.ok(result);
});
