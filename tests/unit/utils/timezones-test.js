import timezones from 'mobecallcentre/utils/timezones';
import { module, test } from 'qunit';

module('Unit | Utility | timezones');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timezones();
  assert.ok(result);
});
