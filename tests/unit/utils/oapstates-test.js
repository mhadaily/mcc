import oapstates from 'mobecallcentre/utils/oapstates';
import { module, test } from 'qunit';

module('Unit | Utility | oapstates');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = oapstates();
  assert.ok(result);
});
