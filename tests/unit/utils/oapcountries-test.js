import oapcountries from 'mobecallcentre/utils/oapcountries';
import { module, test } from 'qunit';

module('Unit | Utility | oapcountries');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = oapcountries();
  assert.ok(result);
});
