{{#if (or (eq application.model.account.team "Phone Sales & Training / L1 Business Coach") (eq application.model.account.role.name "superadmin"))}}

  {{announcement-widget channel="Coach"}}

{{/if}}
{{#if (or (eq application.model.account.team "Phone Sales & Training / L2 Phone Sales") (eq application.model.account.role.name "superadmin"))}}

  {{announcement-widget channel="Senior Coach"}}

{{/if}}
{{#if (or (eq application.model.account.team "Phone Sales & Training / Traffic Coach") (eq application.model.account.role.name "superadmin"))}}

  {{announcement-widget channel="Traffic Coach"}}

{{/if}}
