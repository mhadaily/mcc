<div class="modal-content">
  <div class="modal-header">
    <button type="button" class="close"  {{action "cancel"}} aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <h4 class="modal-title">This is </h4>
  </div>
  <div class="modal-body">
	<form class="form" {{action "newNote" on="submit"}}>
	  <div class="form-group">
	   {{textarea value=noteContent class="form-control" rows="8"}}
	  </div>
	 <button type="button" class="btn btn-primary btn-lg btn-block" {{action "complete"}}>Complete Task and Save Note</button>
	</form>
  </div>
  <div class="modal-footer">
    <button type="button" class="btn btn-default"  {{action "cancel"}}>Close</button>
  </div>
</div><!-- /.modal-content -->
